// LerpSet
// LerpSetLoop
// LerpColor1 (from, to, amount)
// LerpColor2 (amount, colors) - more like LerpSet

// Clamp01 ?

// Otter has a clamp(value, max) that uses 0 as the min

// Vec2.Clamp

// Vec2 approach

// GetPowerSet (every combination of a list)

// enum and bitmask functions?

// collision stuffs
// 		DistanceLinePoint

// bezier curves
// splines

// InRange, Subset

// Vector2.Normal(angle) (returns a Vec2 of length 1 with a given angle) - maybe Vec2.fromAngle?
// Vec2.Distance / .distanceTo
// Vec2.ScaleAround/RotateAround

// PolarX, PolarY

// OneDee, TwoDee, TwoDeeX, TwoDeeY

// Angle(x, y, x2, y2), Angle(from, to), Angle(vec), Angle(Entity a, Entity b) (this last one would be CL-specific)

// SinScale / SinScaleClamp (I think sinRemapClamp)
